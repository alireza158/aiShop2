<header class="main-header"><div class="container"><div class="top-header"><button class="mobile-toggle" type="button" aria-label="منو">☰</button><a href="{{ route('home') }}" class="logo">AI Shop</a><form class="search-box" action="{{ route('products.index') }}" method="GET"><input type="text" name="q" placeholder="جستجو در محصولات دیجیتال..." value="{{ request('q') }}"><button type="submit">جستجو</button></form><div class="header-actions"><a href="{{ url('/login') }}" class="login-btn">ورود | ثبت‌نام</a><a href="{{ route('cart.index') }}" class="cart-btn">🛒 سبد خرید <span>{{ count(session('cart',[])) }}</span></a></div></div>@include('partials.navbar')</div>@include('partials.mobile-menu')</header>
