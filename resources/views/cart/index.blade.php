@extends('layouts.app')
@section('title','سبد خرید')
@section('content')<section class="products-section"><div class="container"><h1 class="section-title">سبد خرید</h1>@if($items->isEmpty())<div class="simple-card">سبد خرید خالی است. <a href="{{ route('products.index') }}">ادامه خرید</a></div>@else<div class="cart-layout"><div>@foreach($items as $item)<div class="cart-item"><img src="{{ \App\Support\ShopImage::url($item['image'] ?? null, 'product') }}"><div><h3>{{ $item['name'] }}</h3><strong>{{ number_format($item['price']) }} تومان</strong></div><form method="POST" action="{{ route('cart.update',$item['id']) }}">@csrf @method('PATCH')<input type="number" name="quantity" value="{{ $item['quantity'] }}" min="1"><button>بروزرسانی</button></form><form method="POST" action="{{ route('cart.remove',$item['id']) }}">@csrf @method('DELETE')<button class="danger">حذف</button></form></div>@endforeach</div><aside class="summary-card"><p>جمع کل: {{ number_format($total) }} تومان</p><p>تخفیف: {{ number_format($discount) }} تومان</p><h3>مبلغ نهایی: {{ number_format($final) }} تومان</h3><a class="btn-main" href="{{ route('checkout.index') }}">تسویه حساب</a><a href="{{ route('products.index') }}">ادامه خرید</a></aside></div>@endif</div></section>@endsection
