@extends('layouts.admin')
@section('title',$title)
@section('content')<div class="card p-3"><div class="d-flex justify-content-between mb-3"><h2 class="h5">{{ $title }}</h2><a class="btn btn-primary" href="{{ route('admin.'.$route.'.create') }}">افزودن</a></div><div class="table-responsive"><table class="table align-middle"><thead><tr><th>#</th><th>عنوان</th><th>وضعیت/جایگاه</th><th>عملیات</th></tr></thead><tbody>@foreach($items as $item)<tr><td>{{ $item->id }}</td><td>{{ $item->title ?? $item->name ?? $item->key }}</td><td>@if(isset($item->is_active))<span class="badge {{ $item->is_active?'bg-success':'bg-secondary' }}">{{ $item->is_active?'فعال':'غیرفعال' }}</span>@endif {{ $item->position ?? '' }}</td><td><a class="btn btn-sm btn-outline-primary" href="{{ route('admin.'.$route.'.edit',$item->id) }}">ویرایش</a><form class="d-inline" method="POST" action="{{ route('admin.'.$route.'.destroy',$item->id) }}">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger" onclick="return confirm('حذف شود؟')">حذف</button></form></td></tr>@endforeach</tbody></table></div>{{ $items->links() }}</div>@endsection
