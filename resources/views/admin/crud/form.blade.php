@extends('layouts.admin')
@section('title','فرم '.$title)
@section('content')<form class="card p-4" method="POST" enctype="multipart/form-data" action="{{ $item->exists?route('admin.'.$route.'.update',$item->id):route('admin.'.$route.'.store') }}">@csrf @if($item->exists)@method('PUT')@endif<div class="row g-3">@foreach($fields as $name=>$type)<div class="col-md-{{ in_array($type,['textarea','editor'])?12:6 }}"><label class="form-label">{{ $name }}</label>@if($type==='textarea')<textarea class="form-control" name="{{ $name }}" rows="4">{{ old($name,$item->$name) }}</textarea>@elseif($type==='editor')<textarea class="form-control js-editor" name="{{ $name }}" rows="8">{{ old($name,$item->$name) }}</textarea>@elseif($type==='checkbox')<div class="form-check form-switch"><input class="form-check-input" name="{{ $name }}" value="1" type="checkbox" @checked(old($name,$item->$name ?? true))></div>@elseif($type==='file')@if($item->$name)<img src="{{ asset($item->$name) }}" class="rounded mb-2" style="max-height:80px">@endif<input class="form-control" type="file" name="{{ $name }}">@else<input class="form-control" type="{{ $type }}" name="{{ $name }}" value="{{ old($name,$item->$name) }}">@endif</div>@endforeach</div><button class="btn btn-primary mt-4">ذخیره</button></form>@endsection
