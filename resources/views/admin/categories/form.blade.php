@extends('layouts.app')
@section('content')<section class="products-section"><div class="container"><h1 class="section-title">فرم دسته‌بندی</h1><form class="simple-card form-grid" method="POST" action="{{ $category->exists?route('admin.categories.update',$category):route('admin.categories.store') }}">@csrf @if($category->exists)@method('PUT')@endif<input name="name" value="{{ $category->name }}" placeholder="نام"><input name="slug" value="{{ $category->slug }}" placeholder="slug"><input name="image" value="{{ $category->image }}" placeholder="تصویر"><textarea name="description">{{ $category->description }}</textarea><label><input type="checkbox" name="is_active" value="1" @checked($category->is_active ?? true)> فعال</label><button>ذخیره</button></form></div></section>@endsection
