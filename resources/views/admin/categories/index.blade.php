@extends('layouts.app')
@section('content')<section class="products-section"><div class="container"><div class="section-head"><h1 class="section-title">مدیریت</h1><a class="btn-main" href="{{ route(Route::currentRouteName()==='admin.products.index'?'admin.products.create':(Route::currentRouteName()==='admin.categories.index'?'admin.categories.create':'admin.sliders.create')) }}">افزودن</a></div><div class="simple-card"><table class="admin-table"><tbody>@foreach(($products ?? $categories ?? $sliders) as $item)<tr><td>{{ $item->name ?? $item->title }}</td><td>{{ $item->is_active ? 'فعال' : 'غیرفعال' }}</td><td><a href="{{ url()->current().'/'.$item->id.'/edit' }}">ویرایش</a></td></tr>@endforeach</tbody></table>{{ ($products ?? $categories ?? $sliders)->links() }}</div></div></section>@endsection
