@extends('layouts.admin')
@section('title','مدیریت محصولات')
@section('content')<div class="card p-3"><form class="row g-2 mb-3"><div class="col-md-3"><input class="form-control" name="q" value="{{ request('q') }}" placeholder="جستجو"></div><div class="col-md-3"><select class="form-select" name="category_id"><option value="">همه دسته‌ها</option>@foreach($categories as $c)<option value="{{ $c->id }}" @selected(request('category_id')==$c->id)>{{ $c->name }}</option>@endforeach</select></div><div class="col-md-3"><select class="form-select" name="brand_id"><option value="">همه برندها</option>@foreach($brands as $b)<option value="{{ $b->id }}" @selected(request('brand_id')==$b->id)>{{ $b->name }}</option>@endforeach</select></div><div class="col-md-3"><button class="btn btn-primary">فیلتر</button><a class="btn btn-success" href="{{ route('admin.products.create') }}">افزودن</a></div></form><table class="table"><tr><th>محصول</th><th>دسته</th><th>قیمت</th><th>موجودی</th><th>وضعیت</th><th></th></tr>@foreach($products as $p)<tr><td>{{ $p->name }}</td><td>{{ $p->category?->name }}</td><td>{{ number_format($p->final_price) }}</td><td>{{ $p->stock }}</td><td><span class="badge {{ $p->is_active?'bg-success':'bg-secondary' }}">{{ $p->is_active?'فعال':'غیرفعال' }}</span></td><td><a class="btn btn-sm btn-outline-primary" href="{{ route('admin.products.edit',$p) }}">ویرایش</a><form method="POST" class="d-inline" action="{{ route('admin.products.destroy',$p) }}">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger">حذف</button></form></td></tr>@endforeach</table>{{ $products->links() }}</div>@endsection
