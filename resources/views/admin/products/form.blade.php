@extends('layouts.app')
@section('content')<section class="products-section"><div class="container"><h1 class="section-title">فرم محصول</h1><form class="simple-card form-grid" method="POST" action="{{ $product->exists?route('admin.products.update',$product):route('admin.products.store') }}">@csrf @if($product->exists)@method('PUT')@endif<select name="category_id">@foreach($categories as $c)<option value="{{ $c->id }}" @selected($product->category_id==$c->id)>{{ $c->name }}</option>@endforeach</select><input name="name" value="{{ old('name',$product->name) }}" placeholder="نام"><input name="slug" value="{{ old('slug',$product->slug) }}" placeholder="slug"><input name="image" value="{{ old('image',$product->image) }}" placeholder="تصویر"><input name="price" value="{{ old('price',$product->price) }}" placeholder="قیمت"><input name="discount_price" value="{{ old('discount_price',$product->discount_price) }}" placeholder="قیمت تخفیف"><input name="stock" value="{{ old('stock',$product->stock ?? 0) }}" placeholder="موجودی"><input name="brand" value="{{ old('brand',$product->brand) }}" placeholder="برند"><textarea name="short_description" placeholder="توضیح کوتاه">{{ old('short_description',$product->short_description) }}</textarea><textarea name="description" placeholder="توضیحات">{{ old('description',$product->description) }}</textarea><label><input type="checkbox" name="is_featured" value="1" @checked($product->is_featured)> ویژه</label><label><input type="checkbox" name="is_active" value="1" @checked($product->is_active ?? true)> فعال</label><button>ذخیره</button></form></div></section>@endsection
