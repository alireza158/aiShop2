@extends('layouts.admin')
@section('title','فرم اسلایدر')
@section('content')<form class="card p-4" method="POST" enctype="multipart/form-data" action="{{ $slider->exists?route('admin.sliders.update',$slider):route('admin.sliders.store') }}">@csrf @if($slider->exists)@method('PUT')@endif<div class="row g-3">@foreach(['title'=>'عنوان','subtitle'=>'زیرعنوان','button_text'=>'متن دکمه','button_link'=>'لینک','display_mode'=>'حالت نمایش','sort_order'=>'ترتیب'] as $n=>$l)<div class="col-md-6"><input class="form-control" name="{{ $n }}" value="{{ old($n,$slider->$n) }}" placeholder="{{ $l }}"></div>@endforeach<div class="col-md-6"><label>تصویر دسکتاپ</label><input class="form-control" type="file" name="image"></div><div class="col-md-6"><label>تصویر موبایل</label><input class="form-control" type="file" name="mobile_image"></div>@foreach(['is_active'=>'فعال','show_mobile'=>'موبایل','show_desktop'=>'دسکتاپ'] as $n=>$l)<div class="col-md-3"><label><input type="checkbox" name="{{ $n }}" value="1" @checked($slider->$n ?? true)> {{ $l }}</label></div>@endforeach</div><button class="btn btn-primary mt-4">ذخیره</button></form>@endsection
