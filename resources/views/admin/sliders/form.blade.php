@extends('layouts.app')
@section('content')<section class="products-section"><div class="container"><h1 class="section-title">فرم اسلایدر</h1><form class="simple-card form-grid" method="POST" action="{{ $slider->exists?route('admin.sliders.update',$slider):route('admin.sliders.store') }}">@csrf @if($slider->exists)@method('PUT')@endif<input name="title" value="{{ $slider->title }}" placeholder="عنوان"><input name="subtitle" value="{{ $slider->subtitle }}" placeholder="زیرعنوان"><input name="image" value="{{ $slider->image }}" placeholder="تصویر"><input name="button_text" value="{{ $slider->button_text }}" placeholder="متن دکمه"><input name="button_link" value="{{ $slider->button_link }}" placeholder="لینک"><input name="sort_order" value="{{ $slider->sort_order }}" placeholder="ترتیب"><label><input type="checkbox" name="is_active" value="1" @checked($slider->is_active ?? true)> فعال</label><button>ذخیره</button></form></div></section>@endsection
